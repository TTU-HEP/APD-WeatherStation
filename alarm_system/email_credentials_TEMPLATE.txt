# email_credentials TEMPLATE
# until we find a better way to handle this, you will need to hardcode in the email password that is used by the email
# that actually sends the alarm. I recommend setting up a dummy email in gmail that sends out the email.

EMAIL_PASSWORD=<PUT PASSWORD HERE>
